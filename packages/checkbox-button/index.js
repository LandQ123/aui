import AfCheckboxButton from '../checkbox/src/checkbox-button.vue';

/* istanbul ignore next */
AfCheckboxButton.install = function(Vue) {
  Vue.component(AfCheckboxButton.name, AfCheckboxButton);
};

export default AfCheckboxButton;
