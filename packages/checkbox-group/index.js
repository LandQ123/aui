import AfCheckboxGroup from '../checkbox/src/checkbox-group.vue';

/* istanbul ignore next */
AfCheckboxGroup.install = function(Vue) {
  Vue.component(AfCheckboxGroup.name, AfCheckboxGroup);
};

export default AfCheckboxGroup;
