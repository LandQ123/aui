import AfButtonGroup from '../button/src/button-group';

/* istanbul ignore next */
AfButtonGroup.install = function(Vue) {
  Vue.component(AfButtonGroup.name, AfButtonGroup);
};

export default AfButtonGroup;
